@inherits WAComponentBase
<wa-qr-code @ref="@Element"
            id="@Id"
            @attributes="@AdditionalAttributes"
            style="@StyleNames"
            class="@ClassNames"
            slot="@Slot"
            value="@Value"
            label="@Label"
            size="@Size"
            fill="@FillColor"
            background="@BackgroundColor"
            radius="@Radius"
            error-correction="@ErrorCorrectionString"
            image="@ImageUrl"
            image-coverage="@ImageCoverage"></wa-qr-code>